Don't  use comma to separate URLS, just use an expandable parameter,
I can just add a + button if users want to see their attributes and links 
easily laidout.

- need testing on the multisource network. Like why is root node optional 
but then when it's empty, the node isn't defaulted to the owner